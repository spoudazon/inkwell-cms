{% extends 'base.html.twig' %}

{% set view = 'page' %}

{% block title %}{{ page.title }} · {{ website.name }}{% endblock %}

{% block content %}
  <article class="post">
    <h1>{{ page.title }}</h1>

    {{ page.body|raw }}
  </article>
{% endblock %}
